version: '3.2'
services:
  my-react-app:
    image: af-reactui-test
    ports:
      - '3000:80'
    environment:
      API_URL: ${API_URL}
      KC_URL: ${KC_URL}
      KC_REALM: ${KC_REALM}
      KC_CLIENTID: ${KC_CLIENTID}
